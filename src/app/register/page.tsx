import RegisterForm from "./RegisterForm";

export default function RegisterPage() {
    return (
      <main className="h-screen flex flex-col justify-center items-center bg-gray-950">
        <RegisterForm />
      </main>
    );
}